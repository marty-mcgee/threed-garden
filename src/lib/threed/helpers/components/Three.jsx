'use client'

// import { r3f } from '@/lib/threed/helpers/global'

// export const Three = ({ children }) => {
//   return <r3f.In>{children}</r3f.In>
// }

import { tunnelratIO } from '@/lib/threed/helpers/global'

export const Three = ({ children }) => {
  return <tunnelratIO.In>{children}</tunnelratIO.In>
}
