export type Event = {
  title: {
    rendered: string
  }
}

// export interface Event {
//   title: Object
// }

export type Scene = {
  title: {
    rendered: string
  }
}

export type Allotment = {
  title: {
    rendered: string
  }
}

export type Bed = {
  title: {
    rendered: string
  }
}

export type Plant = {
  title: {
    rendered: string
  }
}

export type PlantingPlan = {
  title: {
    rendered: string
  }
}
